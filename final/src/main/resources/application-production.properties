# Production configuration for Render - using separate properties
spring.datasource.url=jdbc:postgresql://${DB_HOST:dpg-d5klrf1r0fns738kv6hg-a.oregon-postgres.render.com}:${DB_PORT:5432}/${DB_NAME:bankbd}?sslmode=require
spring.datasource.username=${DB_USER:bankuser}
spring.datasource.password=${DB_PASSWORD:mJGBNEnBoI8bA2gMGT6fc5D8YW7NZWGm}
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
server.port=${PORT:8080}

# Let JPA create tables, then load data
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:data.sql